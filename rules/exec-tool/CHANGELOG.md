# systems/constraints/rules/exec-tool/CHANGELOG.md

This changelog is optional. Git history is the authoritative source if present.
Use this file only if a human-readable change log is helpful.

## 2026-02-25

- Created `exec-tool` pack skeleton (`00-INDEX.md`, template, active rules).
- Added [EXEC-025] to align host/security/approvals semantics with OpenClaw docs.
- Deprecated the legacy monolith as [EXEC-000] for reference/provenance.
